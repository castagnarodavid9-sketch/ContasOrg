<div class="page-wrapper">
  <div class="card">
    <div class="card-header">
      <h2 class="text-center">💲 Débitos</h2>
    </div>

    <div class="card-body">
      <table class="debito-table">
        <thead>
          <tr>
            <th>Descrição</th>
            <th>Valor</th>
            <th>Observação</th>
            <th>Competência</th>
            <th></th>
            <th></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @debitos.each do |d|%>
            <tr>
              <td><%= d.nome_debito%></td>
              <td><%= formata_moedas(d.valor_debito) %></td>
              <td><%= d.descricao %></td>
              <td><%= d.cmpt %></td>
              <td title= "Visualizar"><%= link_to "👁️", debito_path(d), class: "logout-link" %></td>
              <td title= "Editar"><%= link_to "✏️", edit_debito_path(d), class: "logout-link" %></td>
              <td title= "Remover"><%= button_to "🗑️", "#", class: "logout-link" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
